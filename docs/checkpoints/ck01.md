# CK-01 — Session close-out checkpoint
# Date: 2026-05-16
# Sessions completed this run: fix-models + fix-prisma-types (pre-flight cleanup)

## What was done

### SESSION fix-models (order 1 of 17)
Commit: 381b625

Replaced all Cerebras llama3.1-8b usage in scope files with Anthropic SDK per PRD model routing:

| File | Change |
|---|---|
| `app/prompt-engine/adapters/perplexity.adapter.ts` | Stubbed with `NotImplementedException` + TODO comment for fix-perplexity |
| `app/extraction/extraction-haiku.service.ts` | Anthropic SDK, `claude-haiku-4-5-20251001` |
| `app/content-agent/generators/faq-page.generator.ts` | Anthropic SDK, `claude-sonnet-4-20250514` |
| `app/content-agent/generators/comparison-page.generator.ts` | Anthropic SDK, `claude-sonnet-4-20250514` |
| `app/content-agent/generators/entity-authority-page.generator.ts` | Anthropic SDK, `claude-sonnet-4-20250514` |
| `app/content-agent/generators/segment-article.generator.ts` | Anthropic SDK, `claude-sonnet-4-20250514` |

Notes:
- `cerebras.adapter.ts` intentionally untouched — still a valid AiEngine adapter in prompt-engine
- No Cerebras npm package to remove; Cerebras is accessed via `openai` SDK with custom baseURL
- `ANTHROPIC_API_KEY` used via `process.env` — never hardcoded

### SESSION fix-prisma-types (pre-flight, unplanned but necessary)
Commit: fca65ac

Fixed pre-existing environment and test issues uncovered during fix-models:

| Fix | Detail |
|---|---|
| `prisma generate` | Restored Prisma client types. 727 tsc errors → 0 |
| `postinstall` hook | Added to `package.json` so `prisma generate` runs on every `npm install` |
| `ioredis-mock` | Added as devDependency; used in rate-limiter spec to replace live Redis connection |
| `rate-limiter.service.spec.ts` | Replaced `new Redis()` with `new RedisMock()` — 5 timeout failures fixed |
| `content-agent.service.spec.ts` | Added `EventEmitter2` mock provider |
| `prompt-engine.service.spec.ts` | Added `EventEmitter2` mock + `promptRun.count` to mockPrisma |
| `community.service.spec.ts` | Added `EventEmitter2` mock provider |
| `billing/__tests__/webhook.service.spec.ts` | Added `EventEmitter2` mock provider |
| `billing/__tests__/billing.e2e.spec.ts` | Added `EventEmitter2` mock in `buildApp()` |
| `mail.service.spec.ts` | Added `PrismaService` mock provider |

## Final state after this checkpoint

- `tsc --noEmit`: **0 errors**
- Jest: **32/32 suites, 295/295 tests passing**
- Git: clean (all committed and pushed to main)
- HEAD: fca65ac

## What remains

- **Next session: fix-perplexity (order 2 of 17)**
  Scope: `app/prompt-engine/adapters/perplexity.adapter.ts`
  Goal: Replace the `NotImplementedException` stub with a direct Perplexity API integration
  using the `sonar` model via `https://api.perplexity.ai`.

## Key decisions made this session

1. Content generators use `claude-sonnet-4-20250514` (not Haiku) — EXIT CRITERIA overrides the ambiguous GOAL text
2. Extraction uses `claude-haiku-4-5-20251001` — fast, cost-effective for JSON extraction
3. `cerebras.adapter.ts` is NOT in scope and NOT modified — it's a legitimate AiEngine adapter
4. `prisma generate` uses `node node_modules/prisma/build/index.js generate` on Windows (`.bin/prisma` is a bash wrapper that fails under node directly; use `npm run db:generate` or the postinstall hook)
5. EventEmitter2 mock pattern: `{ provide: EventEmitter2, useValue: { emit: jest.fn() } }`

## Env notes (for next session start)
- Run `npm install` (postinstall hook will run `prisma generate` automatically)
- DB: postgres://aeo:aeo_dev@127.0.0.1:5433/aeo_suite
- Redis: redis://localhost:6379 (must be running for app; tests use ioredis-mock)
- Backend: port 3000, Frontend: port 3001
- ANTHROPIC_API_KEY must be set in .env
